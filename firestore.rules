rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /ordonnances/{document} {
      allow read: if true;
      allow create, update, delete: if request.auth != null;
    }
  }
}
